// Product UI and analysis internals have separate contracts. Legacy components are
// retained only during the migration, never mounted into the normal Web surface.
export { default } from "./environment/Workspace";
