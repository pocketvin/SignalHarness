---
description: Run LearningPolicyAgent calibration and review its proposals.
argument-hint: "[--mode demo|mock-agent|agent] [--apply]"
---

Run `signal-harness calibrate $ARGUMENTS`. If `--apply` is requested, require
explicit user confirmation before the policy file is replaced. Summarize the
threshold, keyword, and ignore-pattern changes plus skill/watchlist proposals
and replay evaluation.
