const I18N={zh:{brandSub:'项目相关技术变化雷达 · 可审计 Agent Harness',kicker:'先看变化，再看 Agent 怎么判断',heroTitle:'最近外部技术环境，哪些变化真的影响 {project}？',heroBody:'选择一个项目，实时扫描它的 Watchlist，再用可切换的模型或离线 Agent 判断相关性、影响模块和行动建议。每个结论都能下钻到真实 Trace、Tool Guard 与证据链。',controlTitle:'启动一次变化扫描',project:'关联项目',dataSource:'数据来源',analysis:'分析方式',model:'模型',window:'时间窗口',run:'扫描最新变化',monitoring:'当前监控',changesTitle:'发现了什么变化',changesSub:'先回答“外面发生了什么”，再解释“为什么和项目有关”。',changesEmpty:'点击“扫描最新变化”。实时模式会读取当前 Watchlist；示例模式只用于演示和回归测试。',summaryTitle:'本次扫描',sourceHealth:'来源健康',auditTitle:'Agent 审计过程',auditEmpty:'点击阶段查看 Schema、Tool、Permission、Fallback 与耗时。',step:'步骤 / Agent',status:'状态',duration:'耗时',regression:'回归评测',footerLeft:'实时采集、分析与审计共用同一个 SignalHarness Workflow。',footerRight:'CLI · REST · SSE · MCP · Change Radar',idle:'等待开始',noRun:'暂无运行',creating:'正在创建扫描…',connected:'SSE 已连接',streaming:'正在实时分析',complete:'扫描完成',failed:'扫描失败：{error}',changes:'{count} 条变化',sourceLive:'实时 Watchlist',sourceFixture:'示例数据',analysisMock:'离线五 Agent',analysisAgent:'真实模型',analysisDemo:'确定性基线',window7:'最近 7 天',window14:'最近 14 天',window30:'最近 30 天',providerReady:'已配置 {count} 个真实模型，可按 run 切换。当前：{model}',providerNone:'没有可用真实模型；仍可使用离线五 Agent 或确定性基线。',providerSelected:'当前真实模型：{label} · {model}',statSources:'采集任务',statSuccess:'成功任务',statCollected:'原始事件',statSignals:'分析变化',statRelevant:'值得关注',statAlerts:'提醒/行动',notRun:'尚未运行',running:'运行中',success:'完成',error:'失败',skipped:'跳过',pending:'等待',changeRelease:'版本发布',changeNewIssue:'新增 Issue',changeUpdatedIssue:'Issue 更新',changeRss:'新文章 / 公告',changeUpdatedRss:'文章 / 公告更新',changeWeb:'网页变化',changeOther:'环境变化',whatChanged:'发生了什么',whyRelevant:'为什么与你有关',affected:'可能影响',action:'建议怎么做',source:'来源',published:'发布时间',openSource:'打开原始来源',auditLink:'查看 Agent 审计过程',noAction:'当前无需立即行动，保留观察。',noAffected:'暂无明确受影响模块',sourceQuality:'证据质量',createdAt:'创建',updatedAt:'更新',providerDeprecated:'本地配置使用已下线模型，已安全解析到当前 profile；未修改 .env。',providerUnverified:'当前模型名没有经过能力验证，已使用保守 capability。',decision_alert:'提醒',decision_save:'保存',decision_ignore:'忽略',decision_action_required:'需要行动',stageCollect:'采集 + 标准化',stageSupervisor:'SignalSupervisorAgent',stageEvidence:'ContextEvidenceAgent',stageGuard:'Python 工具守卫',stageImpact:'ImpactAnalystAgent',stageAction:'ActionPlannerAgent',stageLearning:'LearningPolicyAgent',stageDecision:'受控决策',schema:'Schema',fallback:'Fallback',retry:'重试',tools:'工具执行',permissions:'权限检查'},en:{brandSub:'Project-relevant technology change radar · auditable Agent Harness',kicker:'See the change first, then inspect the Agent decision',heroTitle:'Which recent external changes actually matter to {project}?',heroBody:'Choose a project, scan its live Watchlist, then use a selectable real model or offline Agent path to judge relevance, affected modules, and next actions. Every conclusion can be audited through the same Trace and Tool Guard.',controlTitle:'Start a change scan',project:'Project',dataSource:'Data source',analysis:'Analysis',model:'Model',window:'Time window',run:'Scan latest changes',monitoring:'Monitoring',changesTitle:'What changed',changesSub:'First show what happened outside; then explain why it matters to this project.',changesEmpty:'Run a scan. Live mode reads the current Watchlist; fixture mode is only for demos and regression tests.',summaryTitle:'This scan',sourceHealth:'Source health',auditTitle:'Agent audit trail',auditEmpty:'Select a stage to inspect schema, tools, permissions, fallback, and timing.',step:'Step / Agent',status:'Status',duration:'Duration',regression:'Regression evidence',footerLeft:'Live collection, analysis and audit share the same SignalHarness Workflow.',footerRight:'CLI · REST · SSE · MCP · Change Radar',idle:'Idle',noRun:'No active run',creating:'Creating scan…',connected:'SSE connected',streaming:'Analyzing live trace',complete:'Scan complete',failed:'Scan failed: {error}',changes:'{count} changes',sourceLive:'Live Watchlist',sourceFixture:'Fixture data',analysisMock:'Offline five-Agent',analysisAgent:'Real model',analysisDemo:'Deterministic baseline',window7:'Last 7 days',window14:'Last 14 days',window30:'Last 30 days',providerReady:'{count} real models configured and selectable per run. Current: {model}',providerNone:'No real model is configured; offline Agent and deterministic modes remain available.',providerSelected:'Selected real model: {label} · {model}',statSources:'Fetch tasks',statSuccess:'Successful tasks',statCollected:'Raw events',statSignals:'Analyzed changes',statRelevant:'Relevant',statAlerts:'Alert / action',notRun:'Not run yet',running:'running',success:'success',error:'error',skipped:'skipped',pending:'pending',changeRelease:'Release',changeNewIssue:'New issue',changeUpdatedIssue:'Issue updated',changeRss:'Article / notice',changeUpdatedRss:'Article / notice updated',changeWeb:'Web change',changeOther:'External change',whatChanged:'What changed',whyRelevant:'Why it matters',affected:'Affected modules',action:'Suggested action',source:'Source',published:'Published',openSource:'Open source',auditLink:'Inspect Agent audit trail',noAction:'No immediate action; keep under observation.',noAffected:'No specific affected module identified',sourceQuality:'Evidence quality',createdAt:'Created',updatedAt:'Updated',providerDeprecated:'Deprecated local model resolved to the current profile; .env was not modified.',providerUnverified:'This model name has no verified capability profile; conservative capabilities are in use.',decision_alert:'alert',decision_save:'save',decision_ignore:'ignore',decision_action_required:'action required',stageCollect:'Collect + normalize',stageSupervisor:'SignalSupervisorAgent',stageEvidence:'ContextEvidenceAgent',stageGuard:'Python Tool Guard',stageImpact:'ImpactAnalystAgent',stageAction:'ActionPlannerAgent',stageLearning:'LearningPolicyAgent',stageDecision:'Guarded decision',schema:'Schema',fallback:'Fallback',retry:'Retry',tools:'Tool execution',permissions:'Permission checks'}};
const STAGES=[['collect','stageCollect'],['supervisor','stageSupervisor'],['evidence','stageEvidence'],['guard','stageGuard'],['impact','stageImpact'],['action','stageAction'],['learning','stageLearning'],['decision','stageDecision']];
const state={lang:'zh',meta:null,source:null,eventCount:0,traces:new Map(),signals:[],assessments:[],sourceTasks:[],failedSources:[],run:null,selectedStage:null};const $=id=>document.getElementById(id);const t=(k,v={})=>{let s=(I18N[state.lang][k]??k);for(const [a,b] of Object.entries(v))s=s.replace(`{${a}}`,String(b));return s};const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
function safeExternalUrl(value){try{const u=new URL(String(value||''),window.location.href);return ['http:','https:'].includes(u.protocol)?u.href:''}catch{return''}}
function providerWarning(p){if(!p)return'';if(p.warning==='deprecated_model_auto_upgraded')return t('providerDeprecated');if(p.warning==='unverified_model_capabilities')return t('providerUnverified');return''}
function applyLang(){document.documentElement.lang=state.lang==='zh'?'zh-CN':'en';document.querySelectorAll('[data-i18n]').forEach(el=>el.textContent=t(el.dataset.i18n));$('zhBtn').classList.toggle('active',state.lang==='zh');$('enBtn').classList.toggle('active',state.lang==='en');renderControls();renderMeta();renderChanges();renderSummary();renderPipeline();renderTrace();if(state.selectedStage)showStage(state.selectedStage)}
$('zhBtn').addEventListener('click',()=>{state.lang='zh';applyLang()});$('enBtn').addEventListener('click',()=>{state.lang='en';applyLang()});
function renderControls(){const ds=$('dataSource'),am=$('analysisMode'),sd=$('sinceDays');ds.options[0].text=t('sourceLive');ds.options[1].text=t('sourceFixture');am.options[0].text=t('analysisMock');am.options[1].text=t('analysisAgent');am.options[2].text=t('analysisDemo');sd.options[0].text=t('window7');sd.options[1].text=t('window14');sd.options[2].text=t('window30');$('providerField').style.display=am.value==='agent'?'block':'none';$('windowField').style.display=ds.value==='live'?'block':'none';renderModelNote()}
$('analysisMode').onchange=renderControls;$('dataSource').onchange=renderControls;$('providerSelect').onchange=renderModelNote;
function selectedProject(){if(!state.meta)return null;return state.meta.projects.find(p=>p.id===$('projectSelect').value)||state.meta.projects[0]||null}
function renderProjectOptions(){if(!state.meta)return;const select=$('projectSelect');select.innerHTML=state.meta.projects.map(p=>`<option value="${esc(p.id)}">${esc(p.name)}</option>`).join('');const preferred=state.meta.default_project_id;if(preferred&&state.meta.projects.some(p=>p.id===preferred))select.value=preferred;renderMeta()}
$('projectSelect').onchange=()=>{renderMeta();renderSummary()};
function renderMeta(){if(!state.meta)return;const configured=state.meta.providers.filter(p=>p.ready);const project=selectedProject();const watchlist=project?.watchlist||{source_count:0,sources:[]};$('heroTitle').textContent=t('heroTitle',{project:project?.name||(state.lang==='zh'?'当前项目':'this project')});$('badges').innerHTML=`<div class="badge"><strong>${state.meta.projects.length}</strong> ${state.lang==='zh'?'个项目':'projects'}</div><div class="badge"><strong>${watchlist.source_count}</strong> ${state.lang==='zh'?'个来源':'sources'}</div><div class="badge"><strong>${configured.length}</strong> ${state.lang==='zh'?'个真实模型':'real models'}</div><div class="badge"><strong>${state.meta.regression.cases}</strong> ${state.lang==='zh'?'条回归案例':'regression cases'}</div><div class="badge">SSE</div>`;$('watchlistChips').innerHTML=watchlist.sources.map(x=>`<span class="chip">${esc(x.name)}</span>`).join('');$('regressionLabel').textContent=`${state.meta.regression.suite} · ${state.lang==='zh'?'项目级回归，不代表通用模型准确率':'project-specific regression, not a general model benchmark'}`;const r=state.meta.regression;const vals=[[state.lang==='zh'?'案例':'Cases',r.cases],[state.lang==='zh'?'决策':'Decision',pct(r.decision_accuracy)],[state.lang==='zh'?'分类':'Category',pct(r.category_accuracy)],[state.lang==='zh'?'精确率':'Precision',pct(r.priority_precision)],[state.lang==='zh'?'召回率':'Recall',pct(r.priority_recall)]];$('metrics').innerHTML=vals.map(([k,v])=>`<div class="metric"><b>${v}</b><span>${k}</span></div>`).join('');$('mcpLabel').textContent=`${state.meta.mcp.tool_count} ${state.lang==='zh'?'个只读工具 · 权限受控':'read-only tools · permission guarded'}`;$('mcpTools').innerHTML=state.meta.mcp.tools.map(x=>`<span class="chip">${esc(x.replace('signalharness_',''))}</span>`).join('')}
function renderProviderOptions(){if(!state.meta)return;const select=$('providerSelect');const ready=state.meta.providers.filter(p=>p.ready);select.innerHTML=state.meta.providers.map(p=>`<option value="${esc(p.id)}" ${p.ready?'':'disabled'}>${esc(p.label)} · ${esc(p.model||p.profile)}${p.warning?' · ⚠':''}${p.ready?'':' · unavailable'}</option>`).join('');const preferred=state.meta.default_provider_id;if(preferred&&ready.some(p=>p.id===preferred))select.value=preferred;else if(ready[0])select.value=ready[0].id;$('analysisMode').options[1].disabled=!ready.length;renderModelNote()}
function renderModelNote(){if(!state.meta)return;const ready=state.meta.providers.filter(p=>p.ready);const note=$('modelNote');if(!ready.length){note.textContent=t('providerNone');note.className='model-note warn';return}const p=ready.find(x=>x.id===$('providerSelect').value)||ready[0];const base=$('analysisMode').value==='agent'?t('providerSelected',{label:p.label,model:p.model}):t('providerReady',{count:ready.length,model:p.model});const warning=providerWarning(p);note.textContent=warning?`${base} ${warning}`:base;note.className=`model-note ${warning?'warn':'ready'}`}
async function loadMeta(){try{state.meta=await fetch('/demo/meta').then(r=>r.json());renderProjectOptions();renderProviderOptions();renderMeta();renderControls()}catch{$('modelNote').textContent='Metadata unavailable'}}
function resetRun(){if(state.source){state.source.close();state.source=null}state.eventCount=0;state.traces.clear();state.signals=[];state.assessments=[];state.sourceTasks=[];state.failedSources=[];state.run=null;state.selectedStage=null;$('changes').innerHTML=`<div class="empty">${esc(t('changesEmpty'))}</div>`;$('changeCount').textContent=t('changes',{count:0});$('inspector').innerHTML=`<div class="empty">${esc(t('auditEmpty'))}</div>`;renderSummary();renderPipeline();renderTrace();setConnection(t('creating'))}
function setConnection(text,kind=''){$('connectionText').textContent=text;$('dot').className=`dot ${kind}`}
async function startRun(){resetRun();$('runBtn').disabled=true;const mode=$('analysisMode').value;const data_source=$('dataSource').value;const body={project_id:$('projectSelect').value,mode,data_source,since_days:Number($('sinceDays').value),max_events:12,max_events_per_source:4};if(data_source==='fixture')body.fixture='examples/signal_harness/sample_events.json';if(mode==='agent')body.provider_id=$('providerSelect').value;try{const res=await fetch('/stream-runs',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify(body)});const payload=await res.json();if(!res.ok)throw new Error(typeof payload.detail==='string'?payload.detail:JSON.stringify(payload.detail));state.run=payload;$('runId').textContent=payload.run_id;const src=new EventSource(payload.events_url);state.source=src;['run.created','run.started','trace.step','trace.step.updated','run.completed','run.failed'].forEach(name=>src.addEventListener(name,onEvent));src.onopen=()=>setConnection(t('connected'),'live');src.onerror=()=>{if(state.source)setConnection('SSE reconnecting…','error')}}catch(e){setConnection(String(e),'error');$('runBtn').disabled=false}}
$('runBtn').addEventListener('click',startRun);
function onEvent(ev){state.eventCount++;let d={};try{d=JSON.parse(ev.data)}catch{};if(ev.type==='run.started'){state.run=d;setConnection(t('streaming'),'live')}if(ev.type==='trace.step'||ev.type==='trace.step.updated'){state.traces.set(d.index,d.trace);const collect=d.trace?.step==='collect_signals'?d.trace:null;if(collect?.source_tasks)state.sourceTasks=collect.source_tasks;if(collect?.failed_sources)state.failedSources=collect.failed_sources;renderPipeline();renderTrace();renderSummary()}if(ev.type==='run.completed'){state.run=d.run;state.signals=d.signals||[];state.assessments=d.assessments||[];state.sourceTasks=d.source_tasks||state.sourceTasks;state.failedSources=d.failed_sources||state.failedSources;renderChanges();renderSummary();renderPipeline();renderTrace();setConnection(t('complete'),'live');$('runBtn').disabled=false;if(state.source){state.source.close();state.source=null}}if(ev.type==='run.failed'){state.run=d.run;setConnection(t('failed',{error:d.run?.error_class||'error'}),'error');$('runBtn').disabled=false;if(state.source){state.source.close();state.source=null}}$('auditSummary').textContent=`${state.eventCount} SSE · ${state.traces.size} Trace`}
function decisionRank(d){return {action_required:4,alert:3,save:2,ignore:1}[d]||0}function changeKind(e){if(e.change_kind==='released')return'changeRelease';if(e.source_type==='github_issue')return e.change_kind==='updated'?'changeUpdatedIssue':'changeNewIssue';if(e.source_type==='rss')return e.change_kind==='updated'?'changeUpdatedRss':'changeRss';return e.source_type==='web_change'?'changeWeb':'changeOther'}function deltaText(e){if(e.source_type==='github_release'&&e.current_version)return e.previous_version?`${e.previous_version} → ${e.current_version}`:e.current_version;if(e.change_kind==='updated'&&e.source_created_at&&e.source_updated_at)return `${t('createdAt')}: ${formatDate(e.source_created_at)} → ${t('updatedAt')}: ${formatDate(e.source_updated_at)}`;return''}
function renderChanges(){if(!state.signals.length){if(!state.run||state.run.status!=='running')$('changes').innerHTML=`<div class="empty">${esc(t('changesEmpty'))}</div>`;$('changeCount').textContent=t('changes',{count:0});return}const ass=new Map(state.assessments.map(x=>[x.event_id,x]));const rows=state.signals.map(e=>({e,a:ass.get(e.event_id)||{decision:'ignore',impact_score:0,affected_modules:[],action_items:[],reason:''}})).sort((x,y)=>decisionRank(y.a.decision)-decisionRank(x.a.decision)||(y.a.impact_score||0)-(x.a.impact_score||0));$('changeCount').textContent=t('changes',{count:rows.length});$('changes').innerHTML=rows.map(({e,a})=>{const high=['alert','action_required'].includes(a.decision);const actions=(a.action_items||[]).length?(a.action_items||[]).map(x=>`<div>• ${esc(x)}</div>`).join(''):esc(t('noAction'));const mods=(a.affected_modules||[]).length?(a.affected_modules||[]).map(x=>`<span class="tag">${esc(x)}</span>`).join(''):`<span class="tag">${esc(t('noAffected'))}</span>`;const sourceUrl=safeExternalUrl(e.url);const delta=deltaText(e);return `<article class="change ${high?'high':''}"><div class="change-top"><div><span class="change-kind">${esc(t(changeKind(e)))}</span><div class="change-title">${esc(e.title)}</div><div class="source-line"><span>${esc(e.source_name)}</span><span>${esc(formatDate(e.published_at))}</span>${sourceUrl?`<a href="${esc(sourceUrl)}" target="_blank" rel="noopener noreferrer">${esc(t('openSource'))}</a>`:''}</div>${delta?`<div class="delta-line">${esc(delta)}</div>`:''}</div><div><div class="score">${Number(a.impact_score||0).toFixed(1)}</div><span class="tag ${esc(a.decision)}">${esc(t('decision_'+a.decision))}</span></div></div><div class="change-section"><b>${esc(t('whatChanged'))}</b><p>${esc(cleanContent(e))}</p></div><div class="change-section"><b>${esc(t('whyRelevant'))}</b><p>${esc(displayReason(e,a))}</p></div><div class="change-section"><b>${esc(t('affected'))}</b><div class="tag-row">${mods}</div></div><div class="change-section"><b>${esc(t('action'))}</b><p>${actions}</p></div><div class="tag-row"><span class="tag">${esc(t('sourceQuality'))}: ${esc(a.source_quality||'unverified')}</span><span class="tag">${esc(a.category||'unknown')}</span></div><button class="audit-link" type="button" data-action="audit">${esc(t('auditLink'))}</button></article>`}).join('')}
$('changes').addEventListener('click',event=>{const button=event.target.closest('button[data-action="audit"]');if(button)openAudit()});
function cleanContent(e){let s=String(e.content||'');s=s.replace(/```[\s\S]*?```/g,' ');s=s.replace(/^\s*[-*]\s*\[[ xX]\].*$/gm,' ');s=s.replace(/^#{1,6}\s*/gm,'');s=s.replace(/\[([^\]]+)\]\([^)]+\)/g,'$1');s=s.replace(/[`*_>#]/g,' ');s=s.replace(/\s+/g,' ').trim();if(!s||s.length<18)s=e.title||'—';return s.length>430?s.slice(0,427)+'…':s}function categoryTopic(a){const map={checkpoint_persistence_signal:['checkpoint persistence','checkpoint persistence'],tool_calling_signal:['tool registry / Agent Harness','tool registry / Agent Harness'],structured_output_signal:['structured output / schema validation','structured output / schema validation'],provider_compatibility_signal:['provider adapter / API compatibility','provider adapter / API compatibility'],evaluation_benchmark_signal:['Agent Eval / regression','Agent Eval / regression'],security_supply_chain:['security / permission boundary','security / permission boundary'],dependency_update:['dependencies / compatibility','dependencies / compatibility'],ecosystem_issue:['monitored ecosystem / compatibility','monitored ecosystem / compatibility'],competitor_update:['Agent Harness architecture','Agent Harness architecture'],docs_change_signal:['documentation / contract surface','documentation / contract surface'],expert_opinion:['engineering practice / evidence design','engineering practice / evidence design']};return(map[a.category]||[a.category||'project context',a.category||'project context'])[state.lang==='zh'?0:1]}function displayReason(e,a){const raw=String(a.reason||'').trim();if(raw&&!raw.startsWith('Scripted LLM route'))return trimReason(raw);const mods=(a.affected_modules||[]).filter(x=>x!=='project-wide');const topic=mods.length?mods.join('、'):categoryTopic(a);if(state.lang==='zh')return `离线 Agent 将这条变化归到「${a.category||'unknown'}」，与 ${topic} 相关；当前影响分 ${Number(a.impact_score||0).toFixed(1)}，决策为“${t('decision_'+(a.decision||'ignore'))}”。`;return `The offline Agent classified this as ${a.category||'unknown'}, related to ${topic}. Impact score ${Number(a.impact_score||0).toFixed(1)}; decision: ${t('decision_'+(a.decision||'ignore'))}.`}function trimReason(v){const s=String(v||'').trim();return s.length>700?s.slice(0,697)+'…':s||'—'}function formatDate(v){if(!v)return'—';try{return new Intl.DateTimeFormat(state.lang==='zh'?'zh-CN':'en-US',{dateStyle:'medium',timeStyle:'short'}).format(new Date(v))}catch{return v}}
function renderSummary(){const run=state.run||{};const sourceSummary=run.source_summary||{};const relevant=state.assessments.filter(a=>a.decision!=='ignore').length;const alerts=state.assessments.filter(a=>['alert','action_required'].includes(a.decision)).length;const vals=[[t('statSources'),sourceSummary.sources??state.sourceTasks.length],[t('statSuccess'),sourceSummary.successful_sources??state.sourceTasks.filter(x=>x.status==='success').length],[t('statCollected'),sourceSummary.collected_events??state.sourceTasks.reduce((n,x)=>n+(x.output_count||0),0)],[t('statSignals'),state.signals.length],[t('statRelevant'),relevant],[t('statAlerts'),alerts]];$('summaryGrid').innerHTML=vals.map(([k,v])=>`<div class="stat"><span>${esc(k)}</span><b>${v??0}</b></div>`).join('');const projectLabel=run.project_name||selectedProject()?.name||'';const sourceLabel=run.source_mode==='live'?t('sourceLive'):run.source_mode==='fixture'?t('sourceFixture'):t('notRun');const provider=run.provider_id?`${run.provider_id}${run.model?' · '+run.model:''}`:run.mode?run.mode:'';$('scanContext').textContent=[projectLabel,sourceLabel,provider].filter(Boolean).join(' · ');$('sourceHealth').innerHTML=state.sourceTasks.length?state.sourceTasks.map(x=>`<div class="source-row"><span>${esc(x.source_name)}</span><span class="${x.status==='success'?'ok':'bad'}">${esc(x.status)} · ${x.output_count||0}</span></div>`).join(''):'<div class="empty">—</div>'}
function traceForAgent(name){return [...state.traces.values()].filter(x=>(x.agent_name||x.agent||'').includes(name)&&x.step==='llm_agent_call').at(-1)}function guardAggregate(){const arr=[...state.traces.values()];return{tools_requested:arr.flatMap(x=>x.tools_requested||[]),tools_executed:arr.flatMap(x=>x.tools_executed||[]),blocked_tools:arr.flatMap(x=>x.blocked_tools||[]),permission_checks:arr.flatMap(x=>x.permission_checks||[]),tool_errors:arr.flatMap(x=>x.tool_errors||[])} }function stageInfo(id){if(id==='collect'){const x=[...state.traces.values()].find(x=>x.step==='collect_signals');return{x,status:x?'success':state.run?.status==='running'?'pending':'pending'}}if(id==='guard'){const g=guardAggregate();const touched=g.tools_requested.length||g.permission_checks.length;return{x:g,status:touched?(g.tool_errors.length||g.blocked_tools.length?'warn':'success'):(state.run?.status==='success'?'skipped':'pending')}}if(id==='decision')return{x:null,status:state.run?.status==='success'?'success':state.run?.status==='error'?'error':'pending'};const map={supervisor:'SignalSupervisorAgent',evidence:'ContextEvidenceAgent',impact:'ImpactAnalystAgent',action:'ActionPlannerAgent',learning:'LearningPolicyAgent'};const x=traceForAgent(map[id]);if(x)return{x,status:x.status==='error'?'error':(x.fallback_used?'warn':'success')};if(id==='learning'){const deferred=[...state.traces.values()].find(x=>x.step==='learning_deferred');if(deferred)return{x:deferred,status:'skipped'}}if(state.run?.status==='success')return{x:null,status:'skipped'};return{x:null,status:'pending'}}
function renderPipeline(){$('pipeline').innerHTML=STAGES.map(([id,key])=>{const info=stageInfo(id);let meta='';if(id==='guard'){const g=guardAggregate();meta=state.lang==='zh'?`${g.tools_executed.length} 次执行 · ${g.tools_requested.length} 个请求`:`${g.tools_executed.length} executed · ${g.tools_requested.length} requested`}else if(info.x?.duration_ms!=null)meta=`${info.x.duration_ms} ms`;return `<button class="stage ${info.status} ${state.selectedStage===id?'selected':''}" type="button" data-stage="${esc(id)}"><b>${esc(t(key))}</b><span>${esc(t(info.status))}${meta?' · '+esc(meta):''}</span></button>`}).join('')}
$('pipeline').addEventListener('click',event=>{const button=event.target.closest('button[data-stage]');if(button)showStage(button.dataset.stage)});
function showStage(id){state.selectedStage=id;renderPipeline();const info=stageInfo(id);const x=info.x;if(id==='guard'){const g=guardAggregate();$('inspector').innerHTML=`<div class="detail-grid"><div class="kv"><span>${esc(t('tools'))}</span><b>${g.tools_executed.length} / ${g.tools_requested.length}</b></div><div class="kv"><span>Blocked</span><b>${g.blocked_tools.length}</b></div><div class="kv"><span>Errors</span><b>${g.tool_errors.length}</b></div><div class="kv"><span>${esc(t('permissions'))}</span><b>${g.permission_checks.length}</b></div></div><div class="tool-list">${g.tools_executed.map(v=>`<span class="tool">${esc(v)}</span>`).join('')||'<span class="tool">none</span>'}</div><div class="tool-list">${g.permission_checks.map(v=>`<span class="tool">${esc(v)}</span>`).join('')}</div>`;return}if(!x){$('inspector').innerHTML=`<div class="empty">${esc(t(info.status))}</div>`;return}$('inspector').innerHTML=`<div class="detail-grid"><div class="kv"><span>${esc(t('schema'))}</span><b>${x.schema_valid===true?'valid':x.schema_valid===false?'invalid':'n/a'}</b></div><div class="kv"><span>${esc(t('fallback'))}</span><b>${x.fallback_used?'yes':'no'}</b></div><div class="kv"><span>${esc(t('retry'))}</span><b>${x.retry_count||0}</b></div><div class="kv"><span>${esc(t('duration'))}</span><b>${x.duration_ms||0} ms</b></div></div><div class="tool-list">${(x.tools_executed||[]).map(v=>`<span class="tool">${esc(v)}</span>`).join('')}</div><p class="mini">${esc(x.detail||'')}</p>`}
function openAudit(){const d=$('auditDetails');d.open=true;d.scrollIntoView({behavior:'smooth',block:'start'})}
function renderTrace(){const rows=[...state.traces.entries()].sort((a,b)=>a[0]-b[0]);$('traceBody').innerHTML=rows.length?rows.map(([i,x])=>`<tr><td>${i+1}</td><td>${esc(x.agent_name||x.agent||x.step)}<br><span class="mini">${esc(x.step||'')}</span></td><td>${esc(x.status||'')}</td><td>${x.duration_ms||0} ms</td></tr>`).join(''):'<tr><td colspan="4">—</td></tr>';$('auditSummary').textContent=`${state.eventCount} SSE · ${rows.length} Trace`}
function pct(v){return `${Math.round(Number(v||0)*100)}%`}renderControls();renderSummary();renderPipeline();renderTrace();loadMeta();
